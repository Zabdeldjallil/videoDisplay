<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>Accueil</title>
</head>
<body>
    <div class="parent">
    <div class="header">
 <div class="gauche">
     <span><a href="/accueil">TubeYou</a></span>
 </div>
<div class="links">
    <ul>
        <li><a href="/uploading">Upload video </a></li>
    </ul>
</div>
    </div>
    <div class="main">
         
        <% if (locals.videos) { %>
         <% for( let index = 0; index < videos.length; index++ ) { %>
            <div class="vid">
            <img src=<%= videos[index].pathImg %> alt="">
            <div class="title"><a href="/toto/<%= videos[index].pathVid %>"><%= videos[index].title %></a></div>
            </div>
         <% } %>
        <% } %>
    </div>
    <div class="footer">
        footer
    </div>
</div>
</body>
</html>